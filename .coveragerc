[run]
omit =
    *__init__.py,
    tests/*,
    venv/*,
    env/*,
    setup.py,
    sync_generator.py,
    bitcart/providers/jsonrpcrequests.py,
[report]
exclude_lines =
    pragma: no cover
    if not webhook_available:
    def \w*_sync
    raise NotImplementedError()
    if TYPE_CHECKING: